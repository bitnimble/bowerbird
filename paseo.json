{
  "worktree": {
    "setup": [
      "cp $PASEO_SOURCE_CHECKOUT_PATH/.env $PASEO_WORKTREE_PATH/",
      "git submodule update --init --recursive",
      "bun install && bun run build:native",
      "cd web && bun install"
    ]
  }
}
