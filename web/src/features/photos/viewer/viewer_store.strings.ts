// The collection a photograph was opened from names itself: every other label the
// Back button can carry belongs to the page that collection is, and is imported
// from there rather than restated here.
export const ViewerStoreStrings = {
  backToLibrary: () => 'Library',
};
