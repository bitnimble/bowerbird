target/
gen/schemas/
# Regenerated by `tauri android init`, and it bakes in absolute SDK paths.
gen/android/
# Drawn by `scripts/make-icons.ts`, which every build runs first. The mark lives in that
# script rather than as checked-in artwork, so it cannot drift from the page's favicon -
# and an icon that is four rectangles is smaller as code than as three binaries.
icons/
